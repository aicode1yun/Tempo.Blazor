namespace Tempo.Blazor.Abstractions.Models;

/// <summary>Controls where the task name label is rendered relative to the bar.</summary>
public enum GanttTaskNameLocation
{
    InsideBar,
    NextToBar,
    Hidden
}

@namespace Tempo.Blazor.Components.NotionEditor.Blocks.Lists
@using Tempo.Blazor.NotionEditor.Interfaces
@using Tempo.Blazor.NotionEditor.Enums

<div class="tm-notion-numbered @_bgClass"
     data-bg="@Content?.BackgroundColor"
     data-indent="@(Content?.IndentLevel ?? 0)">

    <span class="tm-notion-numbered__number" aria-hidden="true">@_numberLabel</span>

    @if (ReadOnly)
    {
        <div @key="ReadOnly"
             class="tm-notion-numbered__body tm-notion-editable tm-notion-editable--readonly @_alignClass"
             contenteditable="false"
             data-color="@Content?.TextColor">
            @((MarkupString)NotionHtmlSanitizer.SanitizeBlockContent(Content?.Html))
        </div>
    }
    else
    {
        <div @key="ReadOnly"
             class="tm-notion-numbered__body tm-notion-editable @_alignClass"
             contenteditable="true"
             data-placeholder="@Placeholder"
             data-color="@Content?.TextColor"
             spellcheck="true"
             @ref="_editableRef"
             @oninput="() => _dirty = true"
             @onblur="OnBlurAsync"
             @onfocus="HandleFocusAsync">
        </div>
    }

</div>

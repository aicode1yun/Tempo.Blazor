namespace Tempo.Blazor.Components.Feedback;

/// <summary>Color variants for TmSpinner.</summary>
public enum SpinnerColor
{
    /// <summary>Uses the current text color (inherits from parent).</summary>
    Current,

    /// <summary>Primary brand color.</summary>
    Primary,

    /// <summary>White color (for dark backgrounds).</summary>
    White
}

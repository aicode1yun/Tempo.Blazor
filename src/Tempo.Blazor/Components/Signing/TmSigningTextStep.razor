@namespace Tempo.Blazor.Components.Signing
@using Tempo.Blazor.Abstractions.Models

<TmSigningStepShell Field="Field"
                    ValidationMessage="@CurrentValidationMessage"
                    ValidationMessageId="@ValidationDescriptionId"
                    AppearsOn="@AppearsOn"
                    Culture="@Culture"
                    FallbackCulture="@FallbackCulture"
                    Class="@ShellClass"
                    @attributes="AdditionalAttributes">
    @if (IsMultiline)
    {
        <textarea class="tm-signing-text-step__input tm-signing-text-step__textarea"
                  value="@Value"
                  disabled="@Disabled"
                  maxlength="@MaxLength"
                  placeholder="@PlaceholderText"
                  aria-describedby="@ValidationDescriptionId"
                  @onchange="HandleValueChangedAsync"></textarea>
    }
    else
    {
        <input class="tm-signing-text-step__input"
               type="text"
               value="@Value"
               disabled="@Disabled"
               maxlength="@MaxLength"
               placeholder="@PlaceholderText"
               aria-describedby="@ValidationDescriptionId"
               @onchange="HandleValueChangedAsync" />
    }
</TmSigningStepShell>

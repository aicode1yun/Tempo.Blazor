# `document-editor/` JS modules — Phase D

This directory hosts the ES-module port of the legacy monolithic
`../document-editor-wysiwyg.js` (≈27 000 lines). Extraction is incremental: each
module added here removes its inline copy from the monolith once equivalence is
verified.

Build pipeline:

```bash
npm install                       # one-time
npm run build:document-editor     # produces ../document-editor.dist.js
```

## Module layout (target — D2)

| Folder            | Responsibility                                            | Target size  |
|-------------------|-----------------------------------------------------------|--------------|
| `core/`           | helpers, schema registry, model importers/exporters       | ~3 000 lines |
| `history/`        | operation types, command stack, transactions              | ~2 500 lines |
| `layout/`         | paragraphEngine, page metrics, segment generator          | ~5 000 lines |
| `render/`         | atomic renderer, segment patcher                          | ~4 000 lines |
| `input/`          | beforeInput, keydown, composition                         | ~2 500 lines |
| `clipboard/`      | paste pipeline, copy serializer                           | ~1 500 lines |
| `objects/`        | image, table, drawing (lazy-loaded on first object op)    | ~3 000 lines |
| `collaboration/`  | remote ops, CRDT (lazy-loaded on connect)                 | ~2 000 lines |
| `accessibility/`  | announcements, screen-reader help                         | ~500 lines   |
| `runtime/`        | entry point, watchdog, instance manager                   | ~2 000 lines |

## Status (2026-05-28 pass 205 — Phase D session complete)

Migrated (235 modules — `find wwwroot/js/document-editor -name "*.mjs" | wc -l`; 255 Phase D tests passing):
- `core/helpers.mjs` — generic value helpers (hasOwn, clone, shallowClone, …)
- `core/schema.mjs` — `DocumentSchemaRegistry` + `createDefaultSchemaRegistry`
- `core/text-helpers.mjs` — `blockText`, `isEditableTextBlock`, `clampTextBoundary`, `clampTextRange`, `tableColumnCount`
- `core/model-finders.mjs` — `findBlockContainer`, `findCell`, `findTableInfo*`, `findTableBlockByScan`
- `core/normalize-target.mjs` — `normalizeTarget`, `normalizeRange`, `normalizeTextExclusionColumnIndex`
- `core/marks.mjs` — full mark family
- `core/export-types.mjs` — 12 enum mappers for C#-JSON wire format
- `core/inline-runs.mjs` — complete inline run pipeline
- `core/block-import.mjs` — `importParagraphContent`, `importImageObject`, `importTable`, `importBlock`, `importRegion`
- `core/block-export.mjs` — `exportBlock`, `readCommentId`
- `core/comment-revision-export.mjs` — `exportComment`, `exportRevision`
- `core/document-export.mjs` — `exportToCSharpJson` top-level orchestrator
- `core/validate-model.mjs` — `validateModel` (self-contained reference indexing)
- `core/fingerprint.mjs` — `stableJsonString`, `hashStableString`, `createDocumentFingerprint`, `createSelectionDocumentFingerprint` (FNV-1a)
- `accessibility/announcements.mjs` — `createAccessibilityAnnouncer` factory (instance handle + invokeBoundary)
- `core/value-readers.mjs` — `readOptionalBoolean`
- `objects/anchor-region.mjs` — `normalizeAnchorRegionName`, `anchorRegionToValue`, `readObjectLayoutInCell` (precedence resolver)
- `layout/text-exclusion.mjs` — `normalizeTextExclusionPageIndex`, `createTextExclusionScopeKey`, `readTextExclusionScope`
- `core/indexes.mjs` — `createIndexBuilder({ normalizeImageObject })` factory, `createBlockIndexContext`, `findBlockByIndex`
- `core/revision-normalize.mjs` — `normalizeRevisionType/Status/Range` (inbound wire format)
- `core/run-finders.mjs` — `findRunAtOffset`, `inlineAtOffset`, `resolveTextOffsetToInlineIndex`
- `history/transactions.mjs` — `createTransactionsModule({ idCounters, deps })` factory
- `runtime/watchdog-helpers.mjs` — state constants + `computeWatchdogBackoff` + JSON helpers + `createWatchdogContext` + `recordWatchdogEvent`
- `runtime/instance-manager.mjs` — `InstanceManager` class + `defaultInstanceManager`
- `objects/layout-helpers.mjs` — `readObjectWrapSide`, `normalizeRelativePositionName`, `relativePositionToValue`, `verticalAlignmentToValue`, `normalizePositionSpec`, `normalizeLayoutKindName`
- `input/command-marks.mjs` — `normalizeCommandColorValue`, `commandMark`, `isClearValueCommand`
- `render/escape.mjs` — `escapeHtml` (attribute-safe HTML escape)
- `render/run-text.mjs` — `resolveInlineRunDisplayText`, `textFromRunsForRender` (field substitution)
- `clipboard/paste-text.mjs` — `normalizePasteText` (HTML strip + newline normalisation)
- `core/selection-snapshot.mjs` — `createLogicalPosition`, `createLogicalRange`, `normalizeSelectionModeValue`, `normalizeTextSelectionPayload`, `normalizeObjectSelectionPayload`, `isObjectSelectionSnapshot`, `createSelectionSnapshot` (top-level)
- `input/typing-coalescer.mjs` — `shouldCoalesceTyping`, `coalesceTypingOperation`, `defaultCoalesceWindowMs`
- `runtime/instance-results.mjs` — `disposedResult`, `missingResult`, `errorResult` (stable error envelopes)
- `objects/horizontal-position.mjs` — `normalizeHorizontalPositionName`, `horizontalPositionToValue`
- `objects/wrap-mode-value.mjs` — `wrapModeToValue`, `wrapModeToCssName`, `wrapModeCreatesTextExclusion`
- `objects/geometry.mjs` — 10+ rectangle + contour helpers (rectFromGeometry, intersectGeometryRect, normalizeWrapContourPointsForGeometry, createObjectWrapRect, etc.)
- `objects/image-object.mjs` — `normalizeImageObject` + `imageObjectToLayout`
- `objects/sync-image-layout.mjs` — `syncImageLayoutCase` + `applyImageWrapModeToLayout` (Pascal/camel mirror, wrap-mode semantics)
- `objects/drawing-runs.mjs` — `createDrawingRunsModule({buildIndexes})` factory (find/remove drawing runs + snapshot)
- `input/before-input.mjs` — `BeforeInputCommands`, `normalizeBeforeInput`, `createBeforeInputNormalizer`
- `history/operation-classifiers.mjs` — `operationTouchesRevisions`, `operationMayChangeRevisions`
- `history/apply-operation-dispatcher.mjs` — `createApplyOperationDispatcher({handlers, validateOperation, attachOperationMethods, createDiffer, buildIndexes, normalizeRevisionGroups, operationAffectedBlockIds})` factory
- `core/import-orchestrator.mjs` — `createImportOrchestrator({normalizeRevision, buildIndexes})` factory → `importFromCSharpJson`
- `core/first-block.mjs` — `firstTextBlock`, `firstModelSelection`
- `input/autocomplete-trigger.mjs` — `detectAutocompleteTriggerText` (token / mention / slash)
- `input/command-name.mjs` — `compactCommandName`
- `render/floating-position.mjs` — `computeFloatingPosition` (viewport-aware popover placement)
- `history/operation-affected.mjs` — `operationAffectedBlockIds`, `transactionAffectedBlockIds`
- `history/handlers-simple.mjs` — `createSimpleHandlers({findBlock, replaceModelContents, nextSelectionForOperation})` factory
- `history/differ.mjs` — `createDiffer()` change accumulator
- `history/validate-operation.mjs` — `createOperationValidator({findBlock, findDrawingRunByObjectId, attachOperationMethods})` factory
- `core/replace-model.mjs` — `createReplaceModelContents({buildIndexes})` factory
- `core/region-info.mjs` — `findRegionInfoForBlock`, `operationRegionInfo`, `nextSelectionForOperation`
- `core/comment-resolver.mjs` — `commentIdsAtInsertionOffset` (comment inheritance — intersection of left+right run commentIds)
- `core/typing-style.mjs` — `styleHasValues`, `resolveTypingStyleAtInsertion` (typing style inheritance with affinity)
- `core/insert-text-run.mjs` — `insertTextRun(block, offset, text, attributes)` (in-place text-run insert with split/merge/comment-inherit)
- `core/run-mutators.mjs` — pure mutator family (`setParagraphText`, `cloneRunSlice`, `deleteTextRange`, `splitParagraphRuns`, `splitRunsForRange`)
- `history/handlers-text.mjs` — `createTextHandlers({findBlock, revisionById?})` factory (applyInsertText, applyDeleteRangeUntracked, applyMarkOperation, applyMergeParagraph)
- `history/handlers-split.mjs` — `createSplitHandler({findBlockContainer, splitParagraphRuns, importBlock, nextSelectionForOperation, operationRegionInfo})` factory (applySplitParagraph untracked)
- `history/handlers-tracked.mjs` — `createTrackedHandlers({findBlock, findBlockContainer, normalizeRevision, addRevision, setRevisionForRange, setRevisionPayloadText, splitParagraphRuns, importBlock, nextSelectionForOperation, operationRegionInfo})` factory (applyDeleteRangeTracked + applySplitParagraphTracked — Deletion/Structure revisions)
- `history/revision-helpers.mjs` — revisionById, readRevisionStatus/TypeName/MarkerType, setRevisionPayloadText, createTrackedRevisionPayload, transformRunsInRange + 2 factories (createRevisionListHelpers, createSetRevisionForRange)
- `layout/text-measurement.mjs` — `createTextMeasurementService()` synthetic measurer + pure variants (`normalizeMeasureStyle`, `computeMeasureCacheKey`, `measureTextRunPure`)
- `layout/line-breaker.mjs` — `createLineBreakerModule({createTextMeasurementService, normalizeLineBreakerOptions, resolveLineRangesForBreaker, lineRangesAreInvalid, buildLineBreakerFallback, tokensForParagraph, coalesceNonBreakingTokens, normalizeParagraphAlignment, createLineDraft, materializeLineDraft, splitTokenIntoFittingPieces, applyJustifyMetadata})` factory returning `createLineBreaker(measurementService, options)`
- `layout/line-breaker-helpers.mjs` — **pure** support set: `normalizeLineBreakerOptions`, `normalizeLineRanges`, `resolveLineRangesForBreaker`, `isInvalidInterval`, `lineRangesAreInvalid`, `coalesceNonBreakingTokens` (nbsp groups → unbreakable sequence), `splitTokenIntoFittingPieces` (long-token fitting via measurement service), `applyJustifyMetadata` (per-range gap distribution)
- `layout/line-draft.mjs` — `createLineDraft(index, ranges, y)` mutable scaffold + `materializeLineDraft(draft, index, hardBreak, alignment)` final-line builder with computed rect, range shifts and segment heights
- `layout/paragraph-tokenizer.mjs` — full tokenization pipeline: `isCjkCharacter`, `isTokenDelimiter`, `cssLengthToPixels`, `mergeTextStyle` (style + marks fold), `tokenizeText` (newline/tab/nbsp/space/cjk/word/longToken), `runForOffset` + `createParagraphTokenizer({normalizeImageObject})` factory exposing `flattenParagraphRuns` and `tokensForParagraph`
- `layout/paragraph-alignment.mjs` — `normalizeParagraphAlignment(value)` (numeric/string → left/center/right/justify)
- `layout/line-breaker-fallback.mjs` — `createLineBreakerFallback({tokensForParagraph})` factory returning `buildLineBreakerFallback(paragraph, service, options, reason)` (single-line layout below blocked region for invalid-range scenarios)
- `core/selection-token.mjs` — pure readers/normalizers: `normalizeSelectionTokenRegion` (body/header/footer/caption/image/tableCell), `readSelectionTokenValue` (6 known keys + Pascal aliases), `parseSelectionTokenData` (object → cloned/sorted, JSON string → object, invalid → null), `readSelectionTokenData` (chains readers + falls back to `selectionTokenData`)
- `objects/image-resize.mjs` — pure image-resize geometry: `IMAGE_RESIZE_MIN_WIDTH/HEIGHT` (32/24), `normalizeImageResizeHandleName`/`imageResizeHandleIndex` (8 cardinal handles), `computeImageResizeFixedPoint(rect, handle)` (anchor corner opposite to drag handle), `createImageResizeBounds(opts)`, `clampImageResizeSize(width, height, ratio, preserveAspect, bounds)`
- `render/non-printing.mjs` — `formatNonPrintingText(text)` (space → ·, tab → →, newline → ¶\n)
- `render/heading-finder.mjs` — `findActiveHeadingBlockIdFromRects(rects, viewportTop)` (sticky outline highlight)
- `accessibility/labels.mjs` — `formatA11yLabel(template, pageNumber)` ({0} placeholder resolution)
- `core/limit-finder.mjs` — `findLimitForBlock(model, blockId)` (walks body / headers / footers / nested table cells, returns innermost container id, falls back to `'body'`)
- `render/rect-helpers.mjs` — `rectFromAny(rect)` (accepts Pascal/camel/DOMRect-style left+top), `rectContains(rect, x, y)` (inclusive hit-test)
- `runtime/performance-metrics.mjs` — `createPerformanceMetricsHarness()` accumulates baselines + layout-pass counters + memory cleanup snapshot, exposes `recordTypingLatency` / `recordImageDragLatency` / `recordSelectionMovementLatency` / `recordLayoutPass` / `recordMemoryCleanup` / `metrics` / `snapshot` / `dispose`
- `core/schema-validation.mjs` — `schemaAllowsBlockForTest(type, region)` (page-break ⊂ body only, no nested tables in cells) + `normalizeInsertionBlocksForSchema(blocks, region)` (rejects/unwraps via warnings, defaults missing image AltText)
- `input/layout-text-edit-model.mjs` — `applyLayoutTextEditModel(segments, change)` (applies insertText/deleteContentBackward/deleteContentForward/insertParagraph against flat layout-segment text, returns {Handled, Text, CaretOffset, DeletedText?, MergePrevious?/MergeNext?, SplitBefore/SplitAfter})
- `core/formatting-scalar.mjs` — `formattingScalarValue(formatting, commandId, fallback)` (`'mixed'` when selection covers multiple values, otherwise resolved value with fallback)
- `core/stats.mjs` — `median(values)`, `percentileNearestRank(values, p)` (NIST primary method; filters non-finite, sorts ascending)
- `runtime/latency-histograms.mjs` — `createDefaultLatencyBudgets` (canonical ms budgets), `createLatencyHistogramState`, `ensureLatencyHistogramState`, `latencyBudgetForName`, `createLatencyHistogramSummary` (Count/Last/Max/P50/P95/Budget/WithinBudget)
- `runtime/strict-performance-stats.mjs` — `createStrictPerformanceStats()` canonical per-instance counter shape (~110 fields: keyDown/beforeInput/render/layout/marker/objectTrack/clipboard/textExclusion/typingLatency/imageDrag/selectionMovement counters + latencyBudgets/Histograms/lastDetails)
- `render/focus-region.mjs` — DOM walkers: `isElementNode`, `getFocusRegionFromElement(root, element)` (Body/Header/Footer/Image/TableCell + data-render-region override), `getFocusTargetDetails(root, element, region)` (headerFooterId, activeTableCellId/Id, activeImageBlockId, activeCommentId, activeRevisionId, activeObjectId, textBlockId, hitTargetKind)
- `runtime/strict-performance-recorders.mjs` — `recordLatencyHistogram(ensureStats, inst, name, ms, detail?)` (trims to PERFORMANCE_HISTOGRAM_LIMIT=500, captures lastLatencyDetails snapshot, returns histogram summary), `recordPartialRenderScope(ensureStats, inst, opType, scopeIds, detail?)` (dedup + sanitise, trims partialRenderScopeSamples to 100)
- `runtime/strict-performance-helpers.mjs` — `strictPerformanceNow()` (performance.now/Date.now fallback), `normalizePerformanceRegion(value)` (Body/Header/Footer/TableCell/Image/Document), `activeRegionForSelection(selection)`, `activeRegionForInstance(inst)`, `ensureStrictPerformanceStats(inst, createStats)` (lazy-init)
- `runtime/typing-hot-path.mjs` — `typingHotPathWindowMs(inst)` (Pascal/camel option, 100ms floor, 500ms default), `isTypingHotPath(inst, now?)` (true if pendingTypingBoundaryPatches non-empty, suppressCollapsedSelectionChangeUntil in future, OR lastInputDomApplyAt within window+32ms grace)
- `runtime/diagnostics.mjs` — `createDiagnosticsState()` (state shape), `ensureDiagnostics(inst)` (lazy + repairs broken arrays), `recordTimeline(inst, kind, detail?)` (trims to DIAGNOSTICS_TIMELINE_LIMIT=300), `recordDiagnosticError(inst, code, error, detail?)` (lastErrors trim 20 + mirror on timeline + sets `inst.lastError`), `recordWatchdogFailure(inst, kind, error, detail?)` (extends recordDiagnosticError + watchdogFailures trim 20 + pushes `watchdog-recovery-active` warning at ≥2 failures + toggles `data-debug-warning`)
- `runtime/layout-render-metrics.mjs` — `recordLayoutMetric(ensureStats, recordTimelineFn, inst, ms, reason, invalidatedScopes)` (count/last/total/max + invalidatedPages + 'layout-pass' timeline), `recordRenderMetric(ensureStats, recordTimelineFn, inst, ms, reason)` (renderPassCount + fullRenderCount + last/total/max + 'render-pass' timeline)
- `runtime/operation-performance.mjs` — `recordOperationPerformance(ensureStats, recordTimelineFn, inst, operationList, ms, invalidatedScopes, source?)` (inputOperationCount/last/total/max + incremental/fullDocument counters + per-op-type typing latency [InsertText/DeleteRange/Split/Merge] + imageDragLatency [UpdateImageLayout] + lastInputOperationType + 'operation-performance' timeline)
- `render/css-escape.mjs` — `cssEscape(value)` (native CSS.escape when available, basic `"` escape fallback)
- `render/live-block-finder.mjs` — `findLiveTextBlockElement(inst, blockId)`, `findLiveTextBlockElements(inst, blockId, selection?)`, `findLiveTextBlockElementForContext(inst, blockId, context, selection?)`, `liveBlockElementMatchesSelection(node, selection)`, `liveBlockContextFromElement(node)` — DOM queries that find the live text block element for a blockId, optionally filtered by region/headerFooterId/pageIndex context
- `render/dom-selection.mjs` — `selectionBelongsToEditor(inst, selection)` (root.contains check on both endpoints, handles text-node parents), `selectionTargetsTextSurface(inst, selection)` (non-collapsed, common ancestor inside text-surface but not image/figure)
- `render/selection-rect.mjs` — `selectedDomRect(selection)` (union of getClientRects with degenerate filter, fallback to getBoundingClientRect, returns null for collapsed/empty)
- `render/floating-viewport.mjs` — `floatingViewportBoundsAvoidingChrome(options?)` (subtracts toolbar/ribbon top + side-panel right, MIN_WIDTH=320), `floatingViewportWidthAvoidingSidePanel(options?)`; accepts `win`/`doc`/`gutter` injection for testing
- `render/mini-toolbar-predicate.mjs` — `createMiniToolbarPredicate({createSelectionSnapshot})` factory → `shouldShowMiniToolbarForSelectionSnapshot(selection)` (true only for non-collapsed text selections, false for object selections)
- `layout/caret-math.mjs` — `finiteNumber(value, fallback)` (rejects NaN/±Infinity), `caretOffsetFromInterval(interval, x)` (linear x→offset interpolation, handles collapsed via collapsedOffset), `nearestOffsetWithinLine(line, x)` (multi-interval picker, falls back to rect interpolation)
- `layout/hit-rect.mjs` — `hitRectFromAny(rect)` (camel-case `{x,y,width,height}` normaliser, accepts Pascal/DOMRect-style left/top, NaN→0, negative width/height clamped), `hitRectContains(rect, x, y)` (inclusive hit-test, accepts any shape)
- `objects/layer-priority.mjs` — `drawingLayerForWrapMode(wrapMode)` (behind-text/in-front-of-text/object), `hitTestLayerPriority(layerName, wrapMode?)` (z-order: InFrontOfText=30 > default=10 > BehindText=0; layer wins over wrap mode)
- `layout/caret-affinity.mjs` — `inferCaretIntervalAffinity(interval, blockers, fallback)` (decides whether caret attaches BEFORE or AFTER inline blockers like drawing objects, with fallback when ambiguous)
- `layout/layout-block-finder.mjs` — `findLayoutBlockById(layoutBlocks, blockId)` (linear scan by id), `findReferenceLineForOffset(layoutBlock, offset)` (finds line containing offset [start, end] inclusive, falls back to first line)
- `render/page-finder.mjs` — `pageIndexFromPoint(root, x, y)` (returns index of page containing point, or nearest by Euclidean distance; null when no pages or no querySelectorAll)
- `objects/drop-region.mjs` — `normalizeDropRegionName(value, tableCellId?)` (Body/Header/Footer/TableCell, cellId forces TableCell), `anchorRegionForNearestTextPosition(position)` (clamp to 4 regions), `imageAnchorScopeKey(value)` / `imageDropScopeKey(position)` (composite scope keys), `canDropImageInNearestTextScope(object, position, options)` (cross-region drop gating with allowCrossRegionDrop override)
- `input/command-classifiers.mjs` — `commandSource(input)` (toolbar/keyboard/api), `inlineCommandTypes()` / `paragraphCommandTypes()`, `markMatchesCommand(mark, commandId)` (true when mark.type matches the command, handles aliases strike↔strikethrough, textColor↔fontcolor↔foregroundcolor, backgroundColor↔highlight)
- `core/selection-range.mjs` — `createSelectionTextRange({createSelectionSnapshot, createLogicalPosition})` factory → `selectionTextRange(selection)` (anchor+focus → sorted {blockId, start, end, collapsed, selection}, cross-block collapses to focus)
- `accessibility/object-aria.mjs` — `objectAccessibilityIdFragment(value)` (alnum/-_ id sanitiser), `activeObjectStatusId(inst)` (canonical live-region id), `appendAriaDescribedByToken(current, token, enabled)` (add/remove/dedup), `getImageObjectAccessibleLabel(object, fallback?)` (altText > caption > fallback > 'Image'), `objectResizeHandleDirectionLabel(handleName)` (north/south/east/west names), `objectResizeHandleAriaLabel(inst, handleName)` (full a11y label with i18n base)
- `layout/test-text-measurer.mjs` — `testTextMeasureStyle(request)` (Pascal/camel + defaults), `getTextRunMeasureCacheKey(request)` (U+001F-separated stable key), `createTestTextMeasurer()` factory (per-instance cache + `measureTextRun` / `clearTextRunMeasureCache` / `getTextRunMeasureStats`; bold/italic/zoom/letterSpacing scaling)
- `objects/wrap-mode-test.mjs` — JS-test round-trip helpers: `testWrapMode(value)` returns `{value, css}`, `testWrapSide(value)` returns `{value, name, css}` (kebab-case with `both-sides` special case), `testHorizontalPosition(value)` returns `{value, css}` for numeric/string inputs + aliases (start/middle/end), null for invalid
- `history/track-changes.mjs` — `resolveTrackChangesState(options)` (local > global > default merge, returns {displayMode, enabled, localEnabled, globalEnabled, source}), `isTrackChangesEnabled(inst)`, `resolveRevisionUserId(options)` (author.Id > currentUserId > userId > displayName > 'local'), `revisionPayloadText(revision)`, `stableRevisionStringify(value)` (sorted-key JSON for stable comparisons)
- `history/revision-merge.mjs` — `revisionAuthorMergeKey(revision)` (priority chain incl. string author fallthrough), `revisionRunFormattingMergeKey(run)` (formatting fingerprint excluding revision marks), `canMergeAdjacentRevisionRuns(leftRev, rightRev, leftRun, rightRun, leftEnd, rightStart)` (status/type/author/contiguity gate), `replaceRevisionIdOnRun(run, sourceId, targetId)` (rewrites both run.revisionId and revision marks)
- `input/command-id.mjs` — `normalizeCommandId(input)` (resolves string/object/keyboard-event to camel-case command id; strips format./paragraph./toggle- prefixes; ~50 alias chains for strike/fontFamily/color/list/indent/table)
- `input/pending-marks.mjs` — `pendingMarkForCommand(pendingTypingMarks, commandId)` (most-recent matching pending mark, supports alias resolution via markMatchesCommand)
- `core/inherited-style.mjs` — `findInheritedTextColor(block, offset)` (walks paragraph runs; returns run's own color or most-recent left-of-offset fallback)
- `core/object-selection-restore.mjs` — `createObjectSelectionRestorer({createSelectionSnapshot, normalizeTextSelectionPayload, firstModelSelection})` factory → `restoreTextSelectionFromObjectSelection(selection)` (anchor block > textSelection > first-model fallback chain; clears all object-selection metadata)
- `core/range-formatting.mjs` — `createRangeFormatting({transformRunsInRange})` factory → `removeMarksForCommandInRange(block, range, commandId)` (filters matching marks via markMatchesCommand, supports aliases), `clearFormattingInRange(block, range)` (wipes marks + style + renormalises runs)
- `core/runs-for-range.mjs` — `runsForRange(block, range)` (collapsed → single-run via findRunAtOffset, non-collapsed → all intersecting runs, non-paragraph → [])
- `core/blazor-formatting-state.mjs` — `toBlazorFormattingState(formatting)` (adapts engine formatting state to Pascal-cased Blazor shape: tri-state 0/1/2 for bold/italic/underline/strike, numeric paragraph alignment 0..3, Mixed flags, list-type detection, ActiveRegion + IsDisabled + DisabledReason)
- `layout/line-box-scorer.mjs` — `scoreNearestTextPositionLineBox(line, x, y, pointPageIndex?)` (inside line: -10000 bonus, wrong page: +100000 penalty, otherwise Euclidean distance to nearest edge)
- `history/revision-list.mjs` — `createRevisionList({normalizeRevision, buildIndexes})` factory → `ensureRevisionList(model)`, `getRevisionById(model, id)` (camel + Pascal), `addRevision(model, revision)` (upsert by id), `updateRevisionStatus(model, id, status)` (mutates in place + rebuilds indexes)
- `history/revision-run-mutators.mjs` — `createRevisionRunMutators({findBlock, transformRunsInRange, buildIndexes})` factory → `clearRevisionFromRuns(model, revisionId)` (removes revisionId + revision marks, merges adjacent), `removeRevisionRuns(model, revisionId)` (deletes runs containing the revision id), `removeRangeText(model, range)` (clears text on range), `applyRevisionMark(model, range, mark)` (pushes mark via updateMarks)
- `core/split-paragraph-runs.mjs` — `splitParagraphRunsAtOffset(block, offset)` (returns {before, after} with split slices `-split-before`/`-split-after` ids, merges adjacent, plainRuns fallback for empty halves)
- `history/normalize-revision.mjs` — `normalizeRevision(raw)` (Pascal/camel + alias coercion to canonical {id, type, author, authorObject, createdAt, groupId, timestamp, range, affectedRange, payload, payloadJson, status}; string payload → {text}; Action alias maps to status)
- `history/revision-groups.mjs` — `createRevisionGroupNormaliser({ensureRevisionList, buildIndexes})` factory → `normalizeRevisionGroups(model, scopeIds?)` (coalesces adjacent Pending revisions of same author/type/formatting via canMergeAdjacentRevisionRuns; rewrites runs in body/headers/footers/table cells; scope filter excludes 'document'/'revisions' meta-ids; returns {ok, merged, removed, scoped, indexesRebuilt})
- `history/revision-decorative.mjs` — `revisionDecorativeStyle(revision)` (Insertion=green+underline, Deletion=red+strike, FormatChange=purple+underline, unknown={}; payload.decorativeStyle override wins)
- `objects/hit-priority.mjs` — `objectHitPriority(item)` (LayerPriority field wins via finiteNumber, otherwise derives from layer/wrapMode via hitTestLayerPriority)
- `layout/caret-interval.mjs` — `normalizeCaretInterval(line, interval, index)` (turns a raw line interval into canonical caret-interval shape: id/blockId/lineId/pageIndex/region/headerFooterId/tableId/cellId/columnIndex/x/y/width/height/rect/start/end/collapsedOffset/empty/affinity/virtualCaret/objectId; inherits from line when interval omits fields)
- `layout/caret-interval-collector.mjs` — `collectLayoutLineIntervals(layout)` (walks layout.blocks + table cells + headerFooterRegions + pre-flattened lineIntervals → canonical caret-interval array via normalizeCaretInterval), `findCaretIntervalHit(layout, x, y)` (first interval whose rect contains point, null when no match)
- `core/table-cell-factory.mjs` — `createEmptyTableCellFactory({importBlock})` factory → `createEmptyTableCell(tableId, rowIndex, columnIndex)` (canonical cell shape: id=`<tableId>-r<row>-c<col>`, rowSpan/colSpan=1, single empty Paragraph block)
- `core/find-block.mjs` — `createFindBlock({buildIndexes})` factory → `findBlock(model, blockId)` (looks up via model.indexes.blocks cache, lazily rebuilds on miss)
- `core/build-indexes.mjs` — `createBuildIndexes({normalizeImageObject, createBlockIndexContext})` factory → `buildIndexes(model)` (rebuilds model.indexes with blocks/inlines/objects/drawingObjectsById/drawingRunsByBlockId/revisions/comments maps; walks body/headers/footers, recurses into table cells; stamps indexVersion + indexesBuiltAt)
- `history/handlers-paragraph-attribute.mjs` — `createParagraphAttributeHandler({findBlock, nextSelectionForOperation})` factory → `applySetParagraphAttribute(model, op, differ)` (mutates block.content[attributeName], stamps previousValue for undo, emits attributeChange diff)
- `history/handlers-restore-snapshot.mjs` — `createRestoreSnapshotHandler({replaceModelContents, createSelectionSnapshot, firstModelSelection})` factory → `applyRestoreSnapshot(model, op, differ)` (wholesale replace via replaceModelContents, emits restore-snapshot diff, falls back to firstModelSelection)
- `history/handlers-revision-decision.mjs` — `createRevisionDecisionHandler({createRevisionEngine, OperationTypes})` factory → `applyRevisionDecision(model, op, differ)` (dispatches AcceptRevision/RejectRevision to engine.acceptRevision/rejectRevision, emits markerChange diff)
- `history/handlers-table.mjs` — `createTableHandlers({findBlockContainer, findCell, importBlock})` factory → `applyInsertTable(model, op, differ)` (builds N×M Table block via importBlock, inserts after target) + `applyUpdateTableCell(model, op, differ)` (replaces cell.blocks)
- `objects/drawing-snapshot.mjs` — `createDrawingObjectSnapshotFactory({normalizeImageObject})` factory → `createDrawingObjectSnapshot(entry)` (canonical drawing-object snapshot for selection-restore + overlay; objectId/runId/blockId/region/headerFooterId/tableId/cellId/inlineIndex/drawingKind/source/url/assetId/altText/caption/layoutKind/isInline/isAnchored/anchor* + wrapMode/width/height/zIndex/layout)
- `objects/drawing-index.mjs` — `createDrawingIndexHelpers({buildIndexes})` factory → `ensureDrawingIndexes(model)` (lazy-rebuild via buildIndexes when drawing indexes missing) + `rebuildDrawingIndexes(model)` (unconditional rebuild)
- `objects/find-drawing-by-asset.mjs` — `createFindDrawingRunByAsset({ensureDrawingIndexes, normalizeImageObject})` factory → `findDrawingRunByAsset(model, assetId, objectId)` (linear scan over drawingObjectsById, matches by either id, returns null on miss)
- `objects/affected-paragraphs.mjs` — `createAffectedParagraphsAroundObject({findBlockContainer})` factory → `affectedParagraphsAroundObject(model, blockId, options?)` (returns paragraph ids near block that could be affected by object layout; default 3 following; non-paragraph block falls back to index-1)
- `layout/text-exclusion-scope.mjs` — `createTextExclusionScopeDescriptor(options)` (returns disabled `{enabled:false}` when no scope field, else enabled descriptor; explicit scopeKey sets strictScopeKey flag), `textExclusionMatchesScope(exclusion, descriptor)` (scopeKey match wins, strict mode rejects non-key matches, else per-field equality)
- `layout/text-exclusion-factory.mjs` — `createTextExclusion(objectLayout, bodyFrame)` (pure; returns null when wrap mode doesn't create exclusion or final rect is degenerate; TopBottom grows to body width and bails when object is off-body; Tight/Through project wrap contour into a polygon; TableCell + layoutInCell=false collapses scope to Body)
- `objects/anchored-drawing-collector.mjs` — `createAnchoredDrawingRunCollector({normalizeImageObject})` factory → `collectAnchoredDrawingRuns(block, context?)` (skips non-paragraph blocks + inline drawings; falls back to owning block id when anchor missing and lockAnchor=false; stitches region/header-footer/table/cell/columnIndex from context)
- `objects/anchored-drawing-position.mjs` — `resolvePositionReferenceRect(relativeTo,…)`, `resolveAlignedHorizontal(position, reference, width)`, `resolveAlignedVertical(position, reference, height)`, `resolveAnchoredDrawingRect(object, reference, page)` (pure; fixedOnPage collapses paragraph/line/character refs to bodyFrame; width/height clamp at 1)
- `objects/overlap-geometry.mjs` — `intervalEndGeometry`, `subtractGeometryInterval(intervals, blockedLeft, blockedRight, minWidth, atY, lineHeight)` (drops pieces below minWidth), `objectOverlapCollisionRect`, `resolveObjectOverlapGeometry(existing, object, bodyFrame)` (pushes object below colliding peers within same wrap layer; 64-iter guard; allowOverlap short-circuits)
- `objects/anchored-drawing-layout.mjs` — `createAnchoredDrawingResolvers({findLayoutBlockById, findReferenceLineForOffset, drawingLayerForWrapMode, wrapModeCreatesTextExclusion, readObjectLayoutInCell, normalizeTextExclusionColumnIndex, resolveAnchoredDrawingRect})` factory → `resolveAnchoredDrawingReference` (uses paragraph line rect when found, fallback `paragraph-start` band 18px tall when not) + `createAnchoredDrawingLayoutObject` (materialises layout object with layer/region/scope/createsTextExclusion)
- `layout/blocked-intervals.mjs` — `polygonIntervalsAtYGeometry(polygon, y)` (even-odd scan), `mergeGeometryIntervals(intervals, minWidth)` (sort+merge+drop narrow), `polygonBlockedIntervalsForGeometry(polygon, atY, lineHeight, body, minWidth)` (3-sample + vertex-y sampling), `applyWrapSideToBlockedIntervals(intervals, wrapSide, body, minWidth)` (Largest resolves to side with more space), `blockedIntervalsForExclusionGeometry(exclusion, atY, lineHeight, body, minWidth)` (dispatch by kind/wrapMode + wrap-side filter)
- `layout/exclusion-intervals.mjs` — `normalizeManagerInterval(interval, atY, lineHeight, extra)` (canonical shape with defaults; metadata threads via `extra`), `mergeBlockedIntervalsForLayout(intervals, body, minWidth, atY, lineHeight)` (body-clip + merge gaps below minWidth + compose `objectId/blockId` via unique `|`-join, mixed wrapMode/Side → `'Mixed'`), `subtractBlockedIntervalsFromBody(body, atY, lineHeight, blocked, minWidth)` (body row minus blocked, sorted x asc, width desc)
- `layout/text-exclusion-manager.mjs` — `createTextExclusionManager(exclusions, bodyFrame, options)` → `{bodyFrame, scopeKey, exclusions, collectBlockedIntervals, computeAt, resolveLine, getAvailableIntervals}`; `allowOverlap=true` exclusions are ignored; `resolveLine` pushes the line below `blockingBottom` when the original line has no available intervals; `options.intervalCacheStats` counters get incremented in-place
- `layout/available-intervals-cache.mjs` — LRU-ish wrapper around the manager. `getAvailableIntervals(y, height, body, exclusions, minReadableWidth, scopeOptions?)` caches by JSON-stringified canonical signature (256-entry cap), stats stick to the exclusions array via hidden non-enumerable property `__tmAvailableIntervalsCache(Stats)?`; `resetAvailableIntervalsCache(exclusions, reason?)`, `getAvailableIntervalsCacheStats(exclusions)` for inspection
- `layout/wrap-snapshot-intervals.mjs` — `normalizeWrapSnapshotInterval(interval, atY, lineHeight, extra)` (same shape as `normalizeManagerInterval`, kept separate for snapshot output isolation), `collectBlockedIntervalsForWrapSnapshot(exclusions, atY, lineHeight, body, minWidth)` (per-exclusion `blockedIntervalsForExclusionGeometry`, stitches objectId/blockId/wrapMode/wrapSide, sorts x→width)
- `objects/editor-widget.mjs` — `createEditorWidgetFactory({normalizeImageObject})` → `createEditorWidget(block)` (image/table kind, commands list, hitTest that returns `{type:'text'}` for `targetRole === 'text-interval'`); `createImageInspectorStateFactory({normalizeImageObject})` → `createImageInspectorState(block)` (http URL reveals field; missing altText emits `accessibility-warning` badge)
- `objects/image-preview-controller.mjs` — `createImagePreviewControllerFactory({findBlock, normalizeImageObject, imageObjectToLayout, buildIndexes, createParagraphLayoutEngine, affectedParagraphsAroundObject, createOperation, applyOperation, OperationTypes})` → `createImagePreviewController(model, options?)` (drag/resize preview state machine; `lockAspectRatio` keeps width:height ratio; `commit` emits `UpdateImageLayout` and tags result as `transactionType:'preview'`; `cancel` idempotent)
- `layout/paragraph-layout-options.mjs` — `normalizeParagraphLayoutOptions(options)` (canonical `{page,x,y,width,minReadableWidth,lineGap,availableIntervals,resolveAvailableIntervals}`; Pascal/camel; clamps width/minReadableWidth ≥ 1; preserves `resolveAvailableIntervals` function reference)
- `layout/scoped-layout-metadata.mjs` — `createScopedLayoutMetadataDecorator({createBlockIndexContext})` → `decorateScopedLayoutMetadata(layout, context)` (mutates in place; stamps region/headerFooterId/tableId/cellId/columnIndex/pageIndex onto root + lines + segments + caretStops + baselines + objects + exclusions)
- `layout/anchored-drawing-scope.mjs` — `createAnchoredDrawingLayoutScope()` (fresh `{objects, exclusions, anchoredIds: Set}` scope); `createAnchoredDrawingScopeAggregator({createBlockIndexContext, collectAnchoredDrawingRuns, readObjectLayoutInCell, resolveAnchoredDrawingReference, createAnchoredDrawingLayoutObject, resolveObjectOverlapGeometry, createTextExclusion, normalizeTextExclusionColumnIndex, rectFromGeometry, sortObject})` → `addAnchoredDrawingRunsToLayoutScope(block, scope, frame, ctx, fallbackY, laidOutBlocks)` (dedup by objectId, page frame wins for TableCell+layoutInCell=false)
- `layout/segment-style.mjs` — `normalizeLayoutSegmentStyle(style)` (canonical font shape; defaults Arial/16/400/normal; color/backgroundColor null), `decorationsFromMarks(marks)` (underline + strike/strikethrough → `line-through`, dedup), `applySegmentStyleToElement(element, style, decorations)` (DOM mutation; textDecoration set only when decorations present; falsy color preserves existing)
- `layout/paragraph-layout-tree.mjs` — `paragraphRectFromLines(options, lines)` (bounding rect; empty falls back to lineHeight), `firstScopeBlockId(scope)` (affectedScopeIds[0] || scope.blockId), `findLayoutBlock(layout, blockId)`, `createInlineObjectLayoutFromSegmentFactory({normalizeWrapModeName})` → drawing inline-object materialiser (wrapMode defaults to `'Inline'`, createsTextExclusion=false), `createLayoutObjectBlockFactory({normalizeImageObject})` → `layoutObjectBlock(block, opts, version)` (caption height heuristic max(16, min(48, len*0.6)); before/after caret stops)
- `render/render-snapshot.mjs` — `flattenLayoutSegments(layout)`, `stableChecksum(value)` (FNV-1a–like 32-bit hash with `<8-hex>-<length>` suffix; sort-stable JSON; collision-resistant), `createRenderSnapshot(model, layout, selection, options?)` (assembles ok/checksum/fingerprint/affectedScopes; modelVersion/layoutVersion/selectionVersion default to 1)
- `render/render-helpers.mjs` — `domRectToRect`, `rectsOverlap` (strict <), `hasRevisionRun(block)`, `scopeIncludesBlock(scope, blockId, scopeKinds)` (WholeDocument/PageRegion/empty affected list ⇒ match; `'document'` magic id), `markOverlayNonText(node)` (aria-hidden + data-text-probe-ignore)
- `render/model-projections.mjs` — `createModelProjections({blockText, hasRevisionRun})` → `projectEditing(model)` (UI projection w/ imageWidget resize handles + accessibility-warning, paragraph revision-overlay class) + `projectData(model)` (pure data projection, marks/layout/revisions cloned)
- `render/overlay-renderers.mjs` — `createOverlayRenderers({document, markOverlayNonText, asArray, sortObject})` → `renderSelectionOverlay` / `renderRevisionOverlay` / `renderCommentMarkers` / `restoreLogicalSelection` (writes JSON-encoded selection onto root via `data-logical-selection`)
- `core/timing.mjs` — `nowMs()` (`performance.now` w/ fallback to `Date.now`), `elapsedWithSimulated(start, simulated)` (`max(real, simulated)` floor for test determinism)
- `core/selection-to-range.mjs` — `createSelectionToRange({createSelectionSnapshot})` factory → `selectionToRange(selection)` (collapsed → start===end; range → min/max; active table/cell id wins over passive)
- `input/typing-change-buffer.mjs` — `createTypingChangeBufferFactory({attachOperationMethods, shouldCoalesceTyping, coalesceTypingOperation, clone, sortObject})` → `createTypingChangeBuffer(options?)` (1000 ms default coalesce window; `resetForSelectionChange/Enter/Paste/Delete`; `snapshot()` for inspection)
- `render/render-helpers.mjs` adds `rectsOverlapWithTolerance(a, b, tolerance)` (strict overlap test with edge shrink, allows snapping)
- `render/dom-text-mapping.mjs` — `isInlineBreakNode` / `isCaretPlaceholderNode` (`<br data-inline-break>` / `data-caret-placeholder` detection), `domLogicalLength(node)` (text length, inline break = 1, caret placeholder = 0, recursive), `domBoundaryLogicalOffset(root, node, offset)` (DOM→logical offset walk), `createFindTextNodeFactory({document, NodeFilter})` → `findTextNode(root)` (first non-null text node via TreeWalker)
- `layout/caret-rect.mjs` — `createCaretRectFromLayout({normalizeLogicalPosition})` → `caretRectFromLayout(model, layout, position)` (matches `layout.caretStops` on blockId+offset; returns cloned rect or null)
- `core/word-boundary.mjs` — `previousWordBoundary(text, offset)` / `nextWordBoundary(text, offset)` (Ctrl+Left/Right caret nav; skip whitespace then word body / word body then whitespace)
- `layout/object-exclusion-hit-rect.mjs` — `createObjectTextExclusionRectForHitTest({normalizeWrapModeName, wrapModeCreatesTextExclusion, hitRectFromAny, finiteNumber})` → `objectTextExclusionRectForHitTest(item, frame)` (explicit wrapRect wins; else grow object rect by per-side distances/wrapMargin; TopBottom stretches to body width)
- `render/live-paragraph-fast-patch.mjs` — `liveTextNodeCanUseFastPatch(node)` (pure DOM predicate; blocks projected-layout containers + marker/segment/inline children), `createTextBlockHasOnlyPlainTextRuns({isEditableTextBlock, isDrawingRunSource, asArray, asText, readCommentIdsFromRun, readRevisionIdsFromRun})` → `textBlockHasOnlyPlainTextRuns(block)` (true only when every run is plain text w/o marks/comments/revisions/fields/keys)
- `history/dirty-state.mjs` — `createInitialDirtyState()` (fresh dirty-tracking record: epoch/savedEpoch/lastSavedMarker/pendingPatchCount), `getOperationId(operation)` (id/operationId Pascal+camel → string, '' when absent)
- `history/handlers-text-edit.mjs` — `createTextEditHandlers(deps)` → `{applyInsertText, applyDeleteRange}` (the two hot history handlers; insert resolves typing style + registers inline insertion revision; delete supports tracked Pending-Deletion path that leaves text in place vs. untracked removal; both stamp `op.deletedText`/record differ ranges). ~19 injected collaborators
- `history/handlers-image.mjs` — `createImageHandlers(deps)` → `{applyInsertImage, applyUpdateImageLayout, applyMoveDrawingObject, applyUpdateImageMetadata}` (drawing/image operation handlers; insert promotes a paragraph for non-text targets; move normalises old/new layout+anchor then delegates to layout update; ~24 injected collaborators + OperationTypes)
- `layout/object-hit-collectors.mjs` — `createObjectHitCollectors(deps)` → `{collectLayoutObjectHits, collectLayoutTextExclusionHits}` (point hit-testing over layout objects + image blocks; sorted by priority then z-index desc; object-hit skips behind-text + unselectable, exclusion-hit also skips in-front-of-text)
- `render/live-paragraph-text.mjs` — `createSetLiveParagraphText({document, asText})` → `setLiveParagraphText(node, text)` (live-typing fast-path text writer; empty text inserts a `<br data-caret-placeholder>`; false for null node)
- `render/behind-text-surface.mjs` — `createTargetIsBehindTextOverlaySurface({ELEMENT_NODE})` → `targetIsBehindTextOverlaySurface(target)` (true when a pointer lands on a behind-text object overlay so the click falls through to text; interactive chrome inside the overlay short-circuits to false)
- `render/native-caret-range.mjs` — `createNativeCaretRangeFromPoint({document})` → `nativeCaretRangeFromPoint(x, y)` (cross-browser caret hit-test; WebKit `caretRangeFromPoint` then Firefox `caretPositionFromPoint` building a collapsed Range; null when unavailable)
- `render/editable-surface.mjs` — `createEditableSurfacePredicates({ELEMENT_NODE, nativeCaretRangeFromPoint})` → `{targetIsEditableDocumentSurface, nativePointTargetsEditableText}` (pointer-target gating: true only for editable text inside the root and clear of object overlays/toolbars)
- `objects/preview-intervals-signature.mjs` — `normalizePreviewIntervalForCompare(interval)` (`"x:width"` rounded to 2dp), `previewIntervalsSignature(lines)` (per-line `"y|intervals|blockedIntervals"` joined by `;`) — used to skip image-move reflow re-renders when the result is unchanged (jitter-tolerant)
- `objects/image-move-track.mjs` — `captureObjectPointerPreviewNodeState(node)` / `restoreObjectPointerPreviewNodeState(state)` (snapshot+revert preview node inline style + track attrs on drag cancel), `createSerializeImageMoveTrack({clone, sortObject})` → `serializeImageMoveTrack(track)` (deterministic JSON snapshot of a move-track session), `createReadImageMoveTrackOriginalRect({asArray, rectFromGeometry})` → `readImageMoveTrackOriginalRect(session)` (first node bounding rect or object offset/size fallback)
- `objects/image-resize-preview.mjs` — `shouldReanchorImageObject(object, options)` (fixed-on-page never re-anchors; lock-anchored only on explicit drag), `createComputeImageResizePreview({normalizeImageResizeHandleName, clampImageResizeSize, computeImageResizeFixedPoint, clone, sortObject})` → `computeImageResizePreview(track, dx, dy, event)` (per-handle resize geometry; corner/Shift preserve aspect ratio; returns applied top-left delta + new size + fixed pivot)
- `objects/image-move-snap.mjs` — `createComputeImageMoveSnap({rectFromAny, asArray})` → `computeImageMoveSnap(point, context)` (5px snap tolerance; candidates: text-left/right, page-center-x, object-left, line-top; `disableSnap` short-circuits)
- `render/text-position-dom-context.mjs` — `createReadTextPositionDomContext({normalizeDropRegionName, normalizeTextExclusionColumnIndex, anchorRegionForNearestTextPosition})` → `readTextPositionDomContext(root, blockElement)` (walks ancestor chain for `[data-render-region]` / `[data-cell-id]` / `table[data-block-id]` / `.tm-wysiwyg-page[data-page-index]`; returns canonical region/tableId/cellId/columnIndex/pageIndex)
- `layout/nearest-text-position-line-box.mjs` — `createNormalizeNearestTextPositionLineBox(deps)` → `normalize(model, source, index)` (canonicalises drop-target line boxes; null for non-editable blocks or degenerate rects; clamps start/end + per-interval offsets to text length; TableCell + cellId → anchorRegion 'TableCell'; Pascal/camel input)
- `render/block-text-line-rects.mjs` — `createBlockTextLineRectsFromDom({blockText, rectFromGeometry, document?})` → `blockTextLineRectsFromDom(blockElement, block)` (per-line client rects via DOM Range + apportioned char ranges; degenerate rects filtered; falls back to `getBoundingClientRect` when Range fails)
- `render/find-nearest-body-paragraph.mjs` — `findNearestBodyParagraphBlockIdFromPoint(root, x, y)` (closest editable body paragraph to a screen point; object-anchor and zero-height blocks filtered; inside-rect wins by -100000 bias then Euclidean distance to nearest edge tie-breaks)
- `render/object-pointer-target.mjs` — `createGetObjectPointerTarget({ELEMENT_NODE, asText})` → `getObjectPointerTarget(inst, target, event?)` (resolves a pointer target to the floating-object overlay/widget; infers a resize handle name from pointer position within 16px of an overlay corner/edge when no explicit `[data-resize-handle]` matches; `data-object-id` falls back through `data-render-object-id` and `data-block-id`)
- `objects/image-pointer-model-target.mjs` — `createResolveImageObjectPointerModelTarget({findDrawingRunByObjectId, normalizeImageObject})` → `resolveImageObjectPointerModelTarget(inst, pointerTarget)` (looks up the drawing run by objectId; returns `{kind:'drawing', objectId, blockId, object}` or null)
- `objects/image-target-helpers.mjs` — `createImageTargetHelpers(deps)` → `{cloneImageLayoutForTarget, imageTargetOperationTarget, affectedParagraphsForImageTarget, imageTargetCaption}` (shared lookups for the image runtime command path; anchor* scope fields take precedence over target-level ones)
- `objects/insert-image-payload.mjs` — `createFirstDrawingRunFromSourceBlock({read, asArray, isDrawingRunSource})` → scans block inlines for the first drawing run; `createImagePayloadFromInsertImageCommand({compactCommandName, read, firstDrawingRunFromSourceBlock, sortObject})` → extracts `{sourceBlock, image}` from the command body (synthesises an Image from URL fields for `insertimageurl`)
- `objects/image-insert-payload.mjs` — `createNormalizeImageInsertPayload({read, firstDrawingRunFromSourceBlock, asText, stableId, clone, sortObject})` → `normalizeImageInsertPayload(op)` (canonical `{sourceBlock, image, objectId, metadata}` for the InsertImage handler; falls back through ObjectId/BlockId/image.ObjectId/.Id/sourceBlock.Id then synthesises `image-object-<ts>`; metadata stamped with stringified FileName/ContentType/SizeBytes/Provider/ProviderId/UploadId)
- `objects/active-image-target.mjs` — `createActiveImageTarget({asText, findDrawingRunByObjectId, findDrawingRunByAsset, sortObject})` → `activeImageTarget(inst, payload?)` (resolves drawing target from `payload.objectId` / selection's object id / asset id; anchor* scope fields preferred over per-drawing ones)
- `objects/insert-image-selection-target.mjs` — `createSelectionTargetForInsertImageCommand({read, createSelectionSnapshot, firstModelSelection, restoreTextSelectionFromObjectSelection, findBlock, firstTextBlock, isEditableTextBlock, blockText, sortObject})` → `selectionTargetForInsertImageCommand(model, body, currentSelection)` (coerces selection or body.Selection into canonical insertion target; object selection restored to text; falls back to first text block when blockId missing; offset clamped to text length)
- `render/search-markers.mjs` — `normalizeSearchMarkersForRender(blockIds, offsets, lengths)` (canonical `{id, targetId, type:'search', blockId, active, range}` records; accepts parallel-array form OR object array with Pascal/camel variants; drops rows with empty blockId or zero-length range)
- `render/text-overlap.mjs` — `overlapArea(a, b)` (rect intersection area), `createTextOverlapArea({document, NodeFilter})` → `textOverlapArea(root, rect)` (sum of text-line-rect overlaps inside `root`; skips whitespace-only nodes and floating-chrome ancestors)
- `render/wysiwyg-layout-geometry.mjs` — `createIsWysiwygLayoutElementVisible({window?})` (bounding rect + display/visibility/opacity check), `getWysiwygRectRelativeTo(rect, origin)` (translate DOMRect-like by origin; left/top or x/y), `createUnionWysiwygRects({asArray})` (bounding rect of a list, degenerate rects filtered)
- `render/wysiwyg-render-helpers.mjs` — `normalizeScopedBlockIdSet(value)` (Set of block ids or null for full-pass signal), `createWysiwygParagraphProjectionSignature(segments, height, top)` (0.1px-rounded checksum for projection frame-skip), `createObjectEntryPaintLayer({drawingLayerForWrapMode})` → paint layer name from object wrap mode (default Inline)
- `render/header-footer-region.mjs` — `normalizeHeaderFooterScope(scope)` (FirstPage/EvenPage/Primary via substring match), `resolveHeaderFooterRegion(model, type, pageNumber)` (page 1→FirstPage; even→EvenPage; odd→Primary; falls back to Primary then first available)
- `render/inline-style-sanitise.mjs` — `isSafeInlineCssColor` (hex/rgb*/hsl*/keyword), `isSafeInlineFontFamily` (max 160 char allow-list), `normalizeInlineFontSize` (bare number → pt; px/pt/rem/em/% pass through), `createRenderInlineTextHtml({escapeHtml})` → `renderInlineTextHtml(text)` (\n → `<br data-inline-break>`; trailing newline adds caret placeholder)
- `render/inline-formatted-html.mjs` — `createRenderFormattedInlineHtml({markType, markValue, isSafeInlineFontFamily, normalizeInlineFontSize, isSafeInlineCssColor, escapeHtml, renderInlineTextHtml, asArray, asText, unique})` → `renderFormattedInlineHtml(run, chunk, innerHtml?)` (composes bold/italic/under/strike/super/sub/font/color/highlight/link marks into a `<span class="tm-document-inline …">` wrapper; aggregates text-decoration; unwrapped output when no formatting fires)
- `render/marker-span-html.mjs` — `createRenderCommentSpanHtml({escapeHtml, asText})` / `createRenderRevisionSpanHtml({revisionById, readRevisionMarkerType, escapeHtml, asText})` / `createRenderSearchSpanHtml({escapeHtml, asText})` → marker `<span>` builders with `aria-current`, `data-marker-id` and legacy `data-testid` attrs (revision wraps inner `document-wysiwyg-revision-<kind>` span; active marker toggles `--selected`/`--active`)
- `render/inline-drawing-helpers.mjs` — `inlineDrawingIsSelected(inst, block, run, object)` (true when the inline drawing's `objectId` matches `selection.activeObjectId` / `objectId` / `objectSelection.objectId`; empty id never matches), `createRenderDrawingObjectTestMarkerHtml({escapeHtml, asText})` → `renderDrawingObjectTestMarkerHtml(objectId)` (sr-only `<span data-testid="document-wysiwyg-drawing-object-<id>">` marker; empty id → empty string)
- `render/object-aria-html.mjs` — `createRenderObjectSelectionDescriptionAttribute({escapeHtml, activeObjectStatusId})` (emits ` aria-describedby="<id>"` when selected), `createRenderObjectResizeHandleHtml({escapeHtml, objectResizeHandleAriaLabel})` (focusable `role=button` w/ aria-label when selected, `aria-hidden` otherwise), `createRenderObjectFocusPolicyAttributes({escapeHtml, createObjectFocusPolicy})` (emits `data-object-focus-policy` + `aria-selected` + `data-object-selected` when active)
- `render/image-layout-bubble.mjs` — `createRenderImageLayoutBubbleButton({escapeHtml})` (button with `data-testid`/`data-command`/`aria-label`/`aria-pressed` + arbitrary extra attrs; aria-label / ariaLabel both accepted), `createRenderImageLayoutBubbleHtml({normalizeWrapModeName, renderImageLayoutBubbleButton})` (toolbar of 9 buttons: Inline/Wrap/Tight/Break/Behind/Front + Move/Fix + More; active button derived from wrapMode + fixedOnPage + moveWithText)
- `render/drawing-figure-style.mjs` — `createExplicitObjectLayerRect` / `createObjectLayerRectFromObject` ({rectFromGeometry}); `createRenderDrawingFigureStyle({objectLayerRectFromObject})` (inline→inline-block w/ baseline, anchored→absolute + max-width:none); `estimateInlineDrawingCaptionReserveHeight(object, width)` (margin+lines×15px capped at 6 lines); `createRenderDrawingAnchorReservationStyle({estimateInlineDrawingCaptionReserveHeight})` (inline carries real size, anchored collapses to 0×0 visibility:hidden)
- `render/image-figure-classes.mjs` — `createRenderImageFigureClasses({normalizeWrapModeName})` → `renderImageFigureClasses(selected, object)` (wrap-mode→`--wrap-<modeClass>` w/ top-bottom/behind-text/in-front-of-text hyphenated; horizontalPosition.align→position-{left,center,right}; selected→both `--selected` classes)
- `render/dom-text-point.mjs` — `createFirstTextPointInElement({document, NodeFilter})` (TreeWalker DFS to first text descendant, clamps offset), `createProjectedDomTextPointAtBlockOffset({firstTextPointInElement})` (queries `.tm-wysiwyg-layout-segment--projected[data-model-start][data-model-end]`; falls back to closest before/after segment when offset lies in a gap)
- `render/find-layout-object-for-render.mjs` — `findLayoutObjectForRender(layout, objectId, blockId)` (requires authoritative layout w/ `pageMetrics` or `debug.source` starting with `paragraph-layout`; walks `layout.objects` + header/footer regions; matches by `objectId` or `(anchorBlockId, objectId)` pair)
- `render/collect-wysiwyg-page-objects.mjs` — `createCollectWysiwygPageObjectEntries({isDrawingRunSource, createWysiwygObjectRenderEntry})` → `collectWysiwygPageObjectEntries(inst, blocks)` (walks paragraph blocks, picks drawing runs or `isDrawingRunSource` matches, calls factory per match, drops nulls)
- `render/wysiwyg-object-render-entry.mjs` — `createWysiwygObjectRenderEntryFactory({normalizeImageObject, findLayoutObjectForRender, rectFromGeometry, inlineDrawingIsSelected, asText, sortObject})` → `createWysiwygObjectRenderEntry(inst, block, run, inlineIndex, sourceKind?)` (returns `{sourceKind, blockId, runId, inlineIndex, objectId, object, selected}`; authoritative layout rect + pageIndex override; null when no objectId derivable)
- `render/sync-wysiwyg-object-layer-positions.mjs` — `createSyncWysiwygObjectLayerPositions({resolveWysiwygObjectLayerRect, applyWysiwygObjectLayerRect, cssEscape})` → `syncWysiwygObjectLayerPositions(root)` (post-render repositions each object layer item against its live layer rect + mirrors onto matching selection/guides overlays; no-op without querySelectorAll)
- `render/engine-table-html.mjs` — `createRenderEngineTableHtml({escapeHtml, asArray, textFromRuns})` → `renderEngineTableHtml(block)` (`<table>`→`<tr>`→`<td role=gridcell colspan/rowspan>` w/ optional background/border/padding style; paragraph children rendered as `<p>`, non-paragraph children skipped; all text escaped)
- `render/markers-for-block.mjs` — `createMarkersForBlock({refreshRuntimeMarkerStore, asArray})` → `{commentMarkersForBlock, revisionMarkersForBlock, searchMarkersForBlock}` (filter markers whose range starts/ends in a block; comment+revision lazily refresh the marker store; revision filtered to `type` starting `'revision'`; search also matches `marker.blockId`)
- `render/object-overlay-html.mjs` — `buildObjectOverlayStyle(rect)` (absolute-positioned CSS), `createRenderWysiwygObjectSelectionOverlayHtml({objectLayerRectFromObject, renderObjectResizeHandleHtml, escapeHtml, asText})` (selection overlay `<div>`; selected adds box + 8 resize handles), `createRenderWysiwygObjectGuidesOverlayHtml({objectLayerRectFromObject, escapeHtml, asText, asArray})` (alignment-guides overlay; one span per guide, orientation default vertical)
- `render/wysiwyg-object-layer-rect.mjs` — `applyWysiwygObjectLayerRect(node, rect)` (writes left/top/width/height + `--tm-layout-object-*` CSS vars), `createResolveWysiwygObjectLayerRect({cssEscape})` → `resolveWysiwygObjectLayerRect(body, layerRect, item)` (computes object-layer-relative rect from data-* attrs + anchor/block geometry; inline / layout-rect / fixed-on-page / Left-Right-Center alignment branches)
- `render/wysiwyg-object-visual-rect.mjs` — `createGetWysiwygObjectVisualRectRelativeTo({isWysiwygLayoutElementVisible, getWysiwygRectRelativeTo, unionWysiwygRects})` → `getWysiwygObjectVisualRectRelativeTo(item, bodyRect)` (union of visible image (or item fallback) + caption rects relative to body; null when nothing measurable)
- `render/projected-wysiwyg-line.mjs` — `groupProjectedWysiwygSegmentsByLine(segments)` (bucket projected segments into lines by y within 20% line-height, sorted), `createProjectedWysiwygLineRenderer({document, asArray, applySegmentStyleToElement})` → `{renderProjectedWysiwygLine, renderProjectedWysiwygSegment}` (DOM builders for text-exclusion-reflowed paragraph lines)
- `render/projected-segment-split.mjs` — `createSplitProjectedWysiwygSegmentsForReflow({createTextMeasurementService, asArray, asText})` → `splitProjectedWysiwygSegmentsForReflow(segments, fallbackStyle)` (tokenizes segments into whitespace/word tokens with measured per-token widths + offsets; returns originals when nothing tokenizes)
- `render/projected-line-intervals.mjs` — `createResolveProjectedWysiwygLineIntervals({getAvailableIntervals, asArray})` → `resolveProjectedWysiwygLineIntervals(y, lineHeight, bodyWidth, frame, allExclusions)` (resolves available intervals on a projected line; uses moved y/intervals when the line had to shift past blocking geometry; full-body-width fallback)
- `render/projected-paragraph-state.mjs` — `restoreWysiwygProjectedParagraph(paragraph)` (reverts a projected paragraph's HTML + classes/attrs/styles; false when not projected), `createShouldProjectWysiwygParagraph({blockText})` → `shouldProjectWysiwygParagraph(paragraph, block)` (true for non-empty paragraph blocks with no marker/comment/revision children)
- `render/projected-segment-reflow.mjs` — `createReflowProjectedWysiwygSegments({asArray, asText, clone, resolveProjectedWysiwygLineIntervals})` → `reflowProjectedWysiwygSegments(segments, paragraphTop, lineHeight, bodyWidth, frame, allExclusions)` (greedy line-breaker flowing tokenized segments through exclusion intervals; drops leading whitespace; jumps to next interval/line when a token won't fit; returns `{segments, lines}`)
- `render/project-paragraph-around-exclusions.mjs` — `createProjectWysiwygParagraphAroundExclusions(deps)` → `projectWysiwygParagraphAroundExclusions(paragraph, block, paragraphTop, bodyWidth, allExclusions, relevantExclusions)` (the projection orchestrator wiring engine layout + split + reflow + group + render; signature-gated DOM rewrite with line markers + projected line spans; ~13 injected collaborators)
- `render/wysiwyg-dom-text-exclusions.mjs` — `createCollectWysiwygDomTextExclusions(deps)` → `collectWysiwygDomTextExclusions(body, bodyRect, frame)` (scans rendered object layer for visible non-inline exclusion-creating objects, measures their live DOM rect, builds text-exclusion records carrying anchorBlockId + objectElement)
- `core/marker-readers.mjs` — `readCommentId`, `readCommentStatus` (1/'resolved' → resolved else open), `commentById`, `createRevisionReaders({normalizeRevisionStatus, normalizeRevisionType})` → `{readRevisionStatus, readRevisionTypeName, readRevisionMarkerType}` (marker token: revisionDeletion/revisionFormat/revisionInsertion). NOTE: distinct from `core/block-export.mjs`'s `readCommentId` — entry.mjs aliases these as `readMarkerCommentId`/etc.
- `core/anchor-ranges.mjs` — `createAnchorRanges({asArray, asText, findBlock, blockText, resolveInlineRunDisplayText})` → `{blockOffsetFromInlineIndex, rangeFromCommentAnchor, rangeFromRevision}` (resolves comment/revision anchors into `{startBlockId, endBlockId, startOffset, endOffset}`; accepts inline-index or flat-offset anchors; clamps to text length; empty ranges extend to length)
- `core/inline-marker-ranges.mjs` — `createInlineMarkerRanges({asArray, resolveInlineRunDisplayText, readCommentIdsFromRun, readRevisionIdsFromRun})` → `{collectInlineCommentRanges, collectInlineRevisionRanges}` (walks body+header+footer paragraphs + table cells, accumulating per-marker-id char ranges; repeated same-block ids widen the range)
- `core/runtime-markers.mjs` — `createRuntimeMarkerBuilders(deps)` → `{buildRuntimeCommentMarkers, buildRuntimeRevisionMarkers}` (one marker per comment / Pending revision with a resolvable range; inline ranges preferred over anchor fallback; revision inserted/original text derived from payload by marker type; priorities 60/50)
- `core/test-projections.mjs` — `blockTypeForTest(block)` (numeric enum or string → `'image'|'table'|'pageBreak'|'paragraph'`), `paragraphRunsForTest(block)` (normalises inlines/runs to `{Id,Text,Start,End,Marks}` with running offsets; synthesizes empty run; Text/FallbackText/Key fallback)
- `core/marker-store.mjs` — `createMarkerStoreFactory({asArray, clone})` → `createMarkerStore(initialMarkers)` (priority-sorted inline-marker store; `byType`/`byBlock`/`overlapping`/`transformText`/`renderClasses`/`remove`; text transforms shift offsets after insert/delete)
- `render/review-display-mode.mjs` — `normalizeReviewDisplayModeClass(mode)` (mode name → host CSS class), `applyReviewDisplayModeClass(root, mode)` (swaps the review-mode host class, leaving other classes intact)
- `history/operation-types.mjs` — `OperationTypes`, `TransactionTypes`, `isTypingLikeTransactionType`
- `history/id-counters.mjs` — `createIdCounters`
- `history/operations.mjs` — `createOperationsModule({ idCounters })` factory + pure classifiers (`supportsOperationHistory`, …)
- `layout/scope-kinds.mjs` — `LayoutScopeKinds`
- `layout/layout-scope.mjs` — `createLayoutScope`, `inferLayoutScopeFromOperation` (per-op-type scope inference)
- `layout/page-metrics.mjs` — `normalizePageBox`, `normalizePageLayoutSettings`, `createPageLayout`, `createPageBreakLayout`, shift helpers (`shiftRectY`, …), field text resolution (`resolveFieldRunText`, `cloneBlockWithResolvedFields`)
- `objects/wrap-modes.mjs` — wrap mode + wrap side enums and normalizers (full legacy aliases) + `wrapSideToValue`
- `objects/drawing-kind.mjs` — `normalizeDrawingKindName`, `exportDrawingKind`
- `runtime/entry.mjs` — bundler entry, re-exports the migrated set

Remaining in legacy IIFE (require dependency-injection refactor — applicable using the same pattern as `core/indexes.mjs`/`history/transactions.mjs`/`accessibility/announcements.mjs`/`history/operations.mjs`):
- `core/import-orchestrator.mjs` — `importFromCSharpJson` top-level (depends on `normalizeRevision` chain with ID generation)
- `history/command-stack.mjs` — undo/redo orchestration (depends on full transaction machinery + selection state)
- `layout/paragraph-engine.mjs`, `layout/segment-generator.mjs` (5 000+ lines combined, DOM measurement, font metrics)
- `render/atomic-renderer.mjs`, `render/segment-patcher.mjs` (DOM-dependent)
- `input/before-input.mjs`, `input/keydown.mjs`, `input/composition.mjs` (instance state + selection + history)
- `clipboard/paste-pipeline.mjs`, `clipboard/copy-serializer.mjs` (DOM HTML parsing, DOCX import)
- `objects/image-pipeline.mjs`, `objects/table-pipeline.mjs`, `objects/drawing-pipeline.mjs` (UI overlays, drag handles, selection mirroring)
- `collaboration/remote-ops.mjs`, `collaboration/crdt.mjs` (remote message pipeline)
- `accessibility/keyboard-handlers.mjs` (`installAccessibilityAndKeyboardHandlers`)
- `runtime/watchdog.mjs` lifecycle wrapper (`_scheduleRecovery`/`_attemptRecovery` reach into `runtime` external global)
- `objects/sync-image-layout.mjs` (`syncImageLayoutCase` — mutates Pascal+camel mirror fields)

The pure-extraction pattern is now exhausted for the categories that don't reach into `_instances` Map closure or DOM lifecycle. The remaining categories can be migrated incrementally in future sprints using factory injection.

The legacy IIFE remains the production source until the extraction reaches a
critical mass; the bundle output (`document-editor.dist.js`) is currently a
verification artifact only.

namespace Tempo.Blazor.Components.Signing;

/// <summary>Determines how document pages are presented in signing document tools.</summary>
public enum DocumentPageViewMode
{
    /// <summary>Render one page at a time with page navigation.</summary>
    SinglePage,

    /// <summary>Render all pages in a vertical continuous list.</summary>
    Continuous
}

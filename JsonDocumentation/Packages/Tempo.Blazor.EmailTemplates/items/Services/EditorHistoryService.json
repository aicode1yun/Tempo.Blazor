{
  "kind": "Class",
  "category": "Services",
  "namespace": "Tempo.Blazor.EmailTemplates.Services",
  "description": "Maintains undo/redo history of document snapshots. Each pushed state is a deep, independent copy, the depth is bounded, and rapid edits sharing a coalesce key within a short window merge into a single undo step (so typing is one undo, not one per keystroke).",
  "packageId": "Tempo.Blazor.EmailTemplates",
  "sourcePath": "src/Tempo.Blazor.EmailTemplates/Services/EditorHistoryService.cs",
  "documentationStatus": "generated",
  "members": [
    {
      "name": "Initialize",
      "kind": "Method",
      "type": "void",
      "description": "Resets the history to a single baseline state.",
      "parameters": [
        {
          "name": "document",
          "type": "EmailTemplateDocument"
        }
      ]
    },
    {
      "name": "Push",
      "kind": "Method",
      "type": "void",
      "description": "Records a new document state. When coalesceKey matches the previous push and falls within the coalesce window, it replaces the current state instead of adding a new step.",
      "parameters": [
        {
          "name": "document",
          "type": "EmailTemplateDocument"
        },
        {
          "name": "coalesceKey",
          "type": "string?",
          "default": "null"
        }
      ]
    },
    {
      "name": "Redo",
      "kind": "Method",
      "type": "EmailTemplateDocument?",
      "description": "Moves one step forward and returns an independent copy of that state, or null."
    },
    {
      "name": "Undo",
      "kind": "Method",
      "type": "EmailTemplateDocument?",
      "description": "Moves one step back and returns an independent copy of that state, or null."
    }
  ],
  "itemName": "EditorHistoryService"
}
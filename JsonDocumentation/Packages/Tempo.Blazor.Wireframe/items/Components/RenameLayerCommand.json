{
  "kind": "Class",
  "category": "Components",
  "namespace": "Tempo.Blazor.Components.Wireframe.Commands",
  "description": "Renames a layer. Undoable — the old name is supplied explicitly so Undo() can restore it.",
  "packageId": "Tempo.Blazor.Wireframe",
  "sourcePath": "src/Tempo.Blazor.Wireframe/Components/Wireframe/Commands/RenameLayerCommand.cs",
  "documentationStatus": "generated",
  "itemName": "RenameLayerCommand",
  "members": [
    {
      "name": "RenameLayerCommand",
      "type": "RenameLayerCommand(WireframeDocument doc, string layerId, string? oldName, string newName)",
      "description": "Creates the command; oldName may be null when the layer had no name."
    },
    {
      "name": "Name",
      "type": "string",
      "description": "Human-readable label shown in undo/redo tooltips."
    },
    {
      "name": "Execute",
      "type": "void Execute()",
      "description": "Applies the change to the document."
    },
    {
      "name": "Undo",
      "type": "void Undo()",
      "description": "Restores the state captured before Execute() ran."
    }
  ],
  "examples": [
    {
      "title": "Use RenameLayerCommand",
      "code": "commandStack.Push(new RenameLayerCommand(document, layer.Id, layer.Name, \"Background\"));"
    }
  ]
}
